For zPlayMe.pd, zita_rev1.* can be copied from
<faust>/examples/puredatadir/ after saying "make puredata" in the
<faust>/examples directory.  If you add zito_rev1.dsp to this project,
remember that it is an "effect", not a "synth", so the needed faust2pd
options in the Makefile are different.
